# ATM Beras — Raspberry Pi Dependencies
# Install dengan: pip install -r requirements.txt

# Web framework untuk Flask local server (jembatan Python ↔ Chromium)
flask==3.0.3
flask-cors==4.0.1

# HTTP client untuk komunikasi dengan Laravel API
requests==2.32.3

# Manajemen .env file untuk konfigurasi & self-provisioning
python-dotenv==1.0.1

# Library RFID RC522 (hanya dibutuhkan di Raspberry Pi)
# Di PC development, baris ini bisa di-comment
# mfrc522==0.0.7

# GPIO Raspberry Pi (otomatis tersedia di Raspberry Pi OS)
# Di PC development, baris ini bisa di-comment
# RPi.GPIO==0.7.1
